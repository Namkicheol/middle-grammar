# 게임 후속 수정 — 2026-09-13

## 로컬 검증 완료

- 야간학교 숫자 선택: native select 클릭이 전체 DOM 재렌더를 유발해 닫히던 현상을 실제 브라우저에서 재현. 큰 숫자 패드로 교체 후 5→2→8 입력 유지 확인.
- 단서 재열람: 이미 확인한 장소와 보관 단서에서 전체 설명을 다시 표시. 자료실 이동 후 교실 단서 재열람 확인.
- 핵심 안내 16px, 터치 버튼 확대, 야간학교 BETA 표시. 390px 넘침 없음. 학생 검수 blocking 없음(안내 반복은 개선 여지).
- 게임 홈 상단 멀티 참여 CTA와 야간학교/멀티/creator 베타 표시. `?join=1`은 학생 입력 화면 직행, teacher 및 resume 우선순위 유지. PC 1280px/휴대폰 390px 배치와 join-form 표시 검증.
- 상태/오디오/홈/멀티 UI 회귀 및 diff 검사 통과. 현재 변경은 미배포.

## 다음: 보물 게임

- 사용자의 뺏기·배수·선물 아이디어는 인원수 고정 요구가 아님. 단순한 조작과 균형을 우선해 수치 결정.
- 자기 보물 전달과 차감 없는 선물/친구 배수는 구분해서 안내.
- 전체 폭탄은 모든 참가자의 전체 보물 0점. 아주 드문 확률과 방당 제한을 적용할 계획.
- 효과별 소리, 읽을 수 있는 결과 안내, 서버 권한 점수 변경과 중복 요청 방지가 수용 기준.

### 보물 게임 로컬 검수 완료

- 행운상자 1개 추가, 기존 확보/도전/약탈/교환 유지. 안전 35%, 2배 15%, 3배 5%, 자기 보물 나눔 20%, 선물 15%, 천사 10%. 친구가 없으면 친구 효과는 안전 보상으로 대체.
- 배수는 미확정 보물에 적용, 증가량 상한 2배 +500/3배 +1000/천사 +500. 결과에 상한 및 실제 전후 수치 표시.
- 전체 폭탄: 참가자 수로 보정한 아주 낮은 확률, 초기/마지막 30초 보호, 방당 최대 한 번 예약. 전원 확정 점수와 미확정 보물만 0, 학습 기록 보존.
- 서버 결과 이벤트 ID로 중복 재생 방지. 초기 snapshot 무음, 새 이벤트 1회, 같은 ID 반복 0회 추가 재생을 브라우저에서 확인.
- 390px 효과 카드 가독성·접기·재열람·넘침 없음, 교사 표시 확인. 학생 검수 ACCEPT.
- 상위 에이전트: typecheck 및 room-engine 58 tests, 기존 멀티 HTTP/WebSocket 흐름, 신규 mystery 2-client peer snapshot/중복 거부 확인. 작업자 보고: Worker 148 tests, question 18 tests 통과.
- 이 변경분은 아직 커밋·푸시·배포되지 않음.

## 보물 게임 완료 후: 전체 게임 진입 단순화 및 실플레이 QA

- 사용자 추가 지시: 홈에서 게임·단원을 고르고 시작하면 즉시 게임으로 진입. 각 게임의 중복 모드/단원 선택 화면을 제거하고 선택한 단원을 보존한다.
- 직접 URL 진입 시에도 명확한 기본값 또는 홈 선택으로 연결하고 빈 화면을 만들지 않는다.
- Luna가 전체 게임의 문항 표시, 입력, 정답/오답 처리, 다음 문제 전환, 재시작과 모바일 화면을 실제 브라우저로 테스트. 블록블라스트 문항 오류를 우선 재현한다.
- 상위 에이전트가 재현 증거와 수정 결과를 검수한다. 현재 보물 게임 작업을 중단하지 않고 이 순서대로 진행한다.

### 진행 중 검증 (전체 완료 아님)

- 읽기 전용 매핑: 솔로 8종, visible 32개 단원/921문항 연결 확인. 야간학교는 자체 퍼즐이므로 단원 선택 대상이 아님. 숨긴 legacy 단원 링크는 새 시작 검증에서 제외한다.
- 상위 브라우저 확인: 홈 레인저스 선택 → be동사 단원 클릭 → 중복 게이트 없이 해당 단원 시작. 390px 정답 1회/오답 1회 후 다음 문제 전환, 입력 잠금 해제, 가로 넘침 없음. 패배 후 다시 진격 버튼은 같은 단원, 정답/응답 0으로 재시작.
- 재실행 통과: arcade-lobby, solo-launchers, classroom 두 어댑터, game HUD exit, Tower state, Sentence Blast state/effects, Speed double/timer, Escape state, treasure UI, multiplayer escape UI.
- 다른 게임의 실제 조작 전수 검수는 진행 중. 위 자동 검사 통과만으로 전체 게임 실플레이 완료를 의미하지 않음.
- 추가 확인: 레인저스 1280px 게임/문항 표시 및 가로 넘침 없음. 학생 검수에서 하단 답 두 번째 줄은 스크롤 필요(비차단 UX 메모).
- 탈출 negative state 실행: 잘못된 428은 교실에 유지, 528로 수정하면 자료실 이동. 자료실 오답 순서는 그 방에 유지, 정답 순서로 수정하면 미로 이동.
- 보스 작업자 브라우저 확인: 390px direct gate 숨김, 게임 표시와 선택지 4개 확인. 보스 정오답/탑 전체 실제 조작은 미검증.
- 브라우저 환경 장애: Playwright CLI에서 단순 page.url 요청도 응답 정지; 기본 Aside 앱도 AppleEvent -1712 timeout. 상위가 자신이 생성한 검사 daemon만 종료. 남은 실플레이 검수를 자동 검사로 대체하여 완료 처리하지 않음.

### 이번 작업 종료 상태

- 진입 코드 수정 완료: 보스/탑/레인저스/두더지 중복 게이트를 solo 초기 표시에서 숨기고 유효한 단원은 자동 시작. 누락/무효/hidden legacy 단원은 해당 모드가 선택된 홈으로 복귀. 교실 시작 대기는 유지.
- Whack/Blast도 공통 시작 query 사용. Blast 무제한(0)은 실제 무제한으로 전달하며 문장 챌린지 주기는 유지. 교실 제한시간 표시는 무제한으로 바뀌지 않음.
- 최종 상위 재실행 PASS: arcade-lobby, solo-launchers, Blast state, Whack launch state, classroom adapters 2종, student room session. 재접속 테스트는 실제 room helper를 사용하도록 보완했고 다른 방 자격 정보 격리 검증 유지.
- 미완료: 보스 정오답 전체, 탑/두더지/블록블라스트/스피드/보글보글의 요청된 실제 브라우저 전수 조작. 블록블라스트 기존 문제 오류의 실기 재현/해결 확정은 하지 않음. 레인저스와 탈출의 위 확인 결과와 구분.
- 로컬 검사 서버 4173/8787 종료. 사용자 창/다른 작업은 정리 대상에서 제외. 코드 변경은 미커밋·미푸시·미배포.

## 컴퓨터 재시작 후 재개 검증

- 수정 파일 보존 확인. Aside CLI 정상 복구, 로컬 4173에서 실제 조작 재개. Worker typecheck/148 tests 및 문항 18 tests 재실행 PASS. 문항은행 32단원/921문항의 문제·선택지·정답 포함 여부도 오류 없음.
- 홈 → 블록블라스트 무제한 클릭 → 선택 단원·seconds=0 전달, HUD ∞ 확인. hidden legacy 링크는 해당 모드가 선택된 홈으로 복귀.
- 블록블라스트: Luna가 선택형/배열형 정오답, 계속, 단어 칩 정리, 재시작 실제 클릭 PASS. 문제 종류/종료 화면만 fixture로 설정. 상세 기록 `docs/qa/blast-whack-browser-20260913.md`.
- 두더지 상위 실제 조작: 정답 +100, 오답 HP 3→2 및 같은 문제 재시도 가능, 이후 정답(골드 보너스 +600)과 다음 문항 진행 확인. 자연 시간초과는 HP 감소/문항 진행, 팝업 다시풀기는 score=0/hp=3/qIdx=0/round=1로 초기화. OX fixture를 선택한 후 실제 O 두더지 클릭도 정답 처리 확인.
- 390×844 검증은 실제 태블릿이 아닌 동일 출처 테스트 iframe 사용: Blast/Whack scrollWidth=390, 가로 넘침 없음. Whack 시작 게이트 숨김 확인.
- 스피드/보글보글 Luna 실제 정오답·재시작 PASS 보고. 무응답 종료 팝업 NaN점 발견하여 별도 최소 수정 진행. 보글보글 첫 문법 찬스 전 종료 사례는 조작/난이도와 구조적 오류를 구분해 추가 점검 중.

### 재개 후 최종 실플레이 결과

- 보스전: Luna 실제 정오답/보스 HP 감소/다음 문항/다시 도전 초기화 PASS. 탑: 상위 실제 정답 층 상승, 오답 하트 감소·정답 안내, 다음 문항, 재시작 동일 단원·하트 3개 확인. Luna 추가 베팅 반반 차단 안내/안전 선택 후 답 활성화/유령 표시 PASS.
- 화면 크기 검증: Luna 보스·탑 390×844/844×390 iframe에서 가로·세로 넘침 없음. 상위 탑 390×844에서 마지막 답 버튼 bottom=815, scrollWidth=390 확인. 스피드/보글보글 390px iframe 가로 넘침 없음(Luna 보고). 물리 태블릿 터치 검증은 아님.
- 블록블라스트 기존 칩 잔류 오류는 이번 실행에서 재현되지 않음. 현재 cleanup 코드와 회귀 테스트, 실제 선택형/배열형 전환 검사를 모두 통과한 것으로 기록하며 모든 상황의 무결함을 보장하지 않음.
- 0문항 종료 NaN은 실제 발견: score-popup.js의 0/0 계산을 0점으로 처리. 0/0·0/10·5/10·10/10 회귀 PASS. 상위 브라우저는 기존 스크립트 캐시가 남아 최신 로컬 파일에 cache-busting query를 붙여 재로딩한 후 `0점 0 / 0 문제 정답` 표시 확인.
- 보글보글 7/8 종료는 항상 발생하는 구조적 오류가 아님. 매칭/제거 성공 시 통과 가능하며 초반 조준 난도·안내 개선 후보로 남김. 검수만으로 게임 규칙/밸런스를 변경하지 않음. 문법 모달 기능은 fixture로 진입하여 실제 답 클릭 검증했고 자연 8발 경로 전체 성공과 구분.
- 이전 레인저스·탈출 검증과 이번 6종 검증을 합쳐 솔로 8종의 대표 핵심 흐름 확인. 32개 단원 모든 문항을 각각 실플레이한 것은 아님. 배포/커밋/푸시는 하지 않음.
- sounds.js 결과 사운드 래퍼의 동일 0/0 계산도 0으로 처리. score-popup/sounds 각각 4개 경계값 테스트 및 최종 게임 회귀 PASS. 상위 검사 탭과 로컬 서버 종료. 지정 Obsidian 운영 노트 경로가 없어 이 저장소 기록만 갱신.
