{
  "name": "middle-grammar-multiplayer-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:questions": "node scripts/build-question-bank.mjs",
    "test": "vitest --config vitest.config.ts",
    "test:questions": "vitest --config vitest.node.config.ts --run",
    "test:all": "npm run test -- --run && npm run test:questions",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.4",
    "@cloudflare/workers-types": "^5.20260905.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "jose": "^6.1.2",
    "uqr": "^0.1.3"
  }
}
